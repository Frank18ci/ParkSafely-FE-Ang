import { PlacaVehicularDirective } from './placa-vehicular.directive';

describe('PlacaVehicularDirective', () => {
  it('should create an instance', () => {
    const directive = new PlacaVehicularDirective();
    expect(directive).toBeTruthy();
  });
});
